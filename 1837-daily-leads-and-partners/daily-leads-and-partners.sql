# Write your MySQL query statement below
SELECT D.DATE_ID, D.MAKE_NAME, COUNT(DISTINCT LEAD_ID) AS UNIQUE_LEADS, COUNT(DISTINCT PARTNER_ID) AS UNIQUE_PARTNERS FROM DAILYSALES AS D GROUP BY D.DATE_ID, D.MAKE_NAME;